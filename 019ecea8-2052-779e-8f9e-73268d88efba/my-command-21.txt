I first type netsh interface ipv4 show excludedportrange protocol=tcp and see:

通訊協定 tcp 連接埠排除範圍

開始連接埠    結束連接埠

----------    ----------

* - 管理的連接埠排除。

And then, I type python -m http.server 8765 and immediately see Windows Defender defending Python, I check the box for private network also. And the Git Bash window continues:

$ python -m http.server 8765

Serving HTTP on :: port 8765 (http://[::]:8765/) ...

I run New-NetFirewallRule -DisplayName "Local web preview 8765" -Direction Inbound -Protocol TCP -LocalPort 8765 -Action Allow -Profile Any in Power Shell with Administrator right.

Finally, I type [http://192.168.0.127:8765](http://192.168.0.127:8765) in Safari. After loading for about 5 seconds, it says server not responding, Safari cannot open the webpage.

Four real bugs. Let me work through each.

## Issue 1 — RevenueCat error in Expo Go

This is informational, not a bug. Expo Go doesn't bundle the native `react-native-purchases` module, so configuring it fails. The error is logged once but doesn't break the app — your wrapper catches it. The cleanest fix is to skip the configure call when running in Expo Go:

## Issue 2 — `<p>` placeholder not replaced in Section-3 Tolerable Maximum Losses (mobile)

Found it. The Section-3 lDescEn contains `<p>` but `DashboardScreen.js` only replaces `<n>` and `<r>`. Fix:

Let me also check for any other places that miss a placeholder. The reverse `rangeDescZh/En` also has both `<r>` and `<p>` — I already do those. Let me audit:

All other replacements look correct.

## Issue 3 — Icon recolouring is destroying multi-colour artwork

The `recolour()` helper in `BottomNav.js` rewrites every `fill="#000"` / `stroke="#000"` to the theme colour. The `privacy-document-icon.svg` and `formula-fx-icon.svg` both lack explicit `fill` attributes — their `<path>` has NO fill set, so SVG defaults to `fill="#000000"`. But my recolour only replaces explicit `fill="#000"` strings, so the actual paths stay black on dark mode.

Also, the user wants to **keep using `p-writing.svg` for web** and only use `privacy-document-icon.svg` as a fallback for mobile. Let me restructure:

Confirmed: neither SVG has explicit `fill="black"` — they rely on the SVG default (black). Same for `privacy-document-icon` which uses CSS class `.st0` containing only `fill-rule:evenodd;clip-rule:evenodd;` (no fill colour).

The cleanest universal fix: **inject a top-level `<g fill="currentColor">` wrapper**, and apply `color` via the `<SvgXml/>` outer style. Or simpler — pass `color` prop to SvgXml which `react-native-svg` then uses as fallback for unset fills.

Actually `react-native-svg`'s `SvgXml` accepts a `color` prop that controls `currentColor`. Better — let me programmatically inject `fill="currentColor"` on every `<path>` / `<polygon>` / `<circle>` / `<rect>` / `<g>` that lacks an explicit fill, AND set the SVG root `color` attribute to our theme colour. This way:
- Black-by-default icons (formula, privacy-document) follow the theme.
- Multi-colour icons (the gear with `fill="#72a7cf"`) keep their colours.

Also, restore `pWriting` export so it remains in the bundle, and use `pWriting` as primary with `privacyDocumentIcon` as fallback:

Now rewrite `BottomNav.js` with proper recolour logic and a `pWriting → privacyDocumentIcon` fallback. Use `ErrorBoundary` around `SvgXml`:

Also restore web `js/icons.js` to include p-writing back:

And revert web's nav mapping to use `pWriting` as primary for Legal (matches the user's wish):

## Issue 4 — Subscript notation lost on mobile

Issue: The `transform: [{ translateY: 4 }]` on a nested `<Text>` inside another `<Text>` is **ignored** on iOS — RN only allows `transform` on `<View>`-like components, not inline `<Text>` ranges. Also, nested `<Text>` styles don't reliably propagate `fontSize` on iOS in all RN versions.

Better approach: render the whole notation as a horizontally laid-out `<View>` row, with the base character using a normal font and sub-characters in a smaller font shifted down via paddingTop. Let me rewrite:

Let me sanity-check all JS files:

All 28 files parse. Commit:

Tagged **v1.0.16**.

## Summary

| # | Issue | Fix |
|---|-------|-----|
| 1 | RevenueCat "Invalid API key" log in Expo Go | `revenuecat.js` now checks `Constants.appOwnership === 'expo'` and skips loading the native module entirely. No more console noise. Real subscription flow continues to work in EAS dev/prod builds. |
| 2 | `<p>` not replaced in Section-3 Tolerable Maximum Losses | `DashboardScreen.js` now does `.replace('<p>', String(calc.pCappedByN(p, n)))` alongside the existing `<n>` and `<r>` substitutions. |
| 3 | Icons rendered all-black on mobile, ignoring dark mode | Root cause: most source SVGs have NO explicit `fill` attribute, so RN paints them black by default. The old `recolour()` only handled explicit `fill="#000"`. New `tintIcon()` injects `fill="currentColor"` + `color="<themeColor>"` on the root `<svg>`, so every shape without an explicit fill inherits the active nav text colour. Multi-colour icons (gear's `#72a7cf`) keep their explicit fills. The privacy-document-icon's two-tone shield (white-left, black-right) now inverts properly in dark mode. |
| 3b | Wanted `p-writing.svg` for web, `privacy-document-icon.svg` as fallback | Restored `p-writing.svg` in both `assets/icons/` folders. Web uses it directly (browsers parse the radial gradients fine). Mobile uses `pWriting` as the primary Legal icon wrapped in a React error boundary; if `SvgXml` throws on the gradient defs, it falls back to `privacyDocumentIcon`. |
| 4 | Subscripts disappeared on mobile | Old `SubSym` used `transform: [{ translateY: 4 }]` on inner `<Text>` — silently ignored by iOS. Rewrote to render each character as a separate `<Text>` inside a flex `<View>` row, with subs sized smaller and shifted down via `marginTop`. Supports double-subscripts (`E_{R_{min}}`, `L_{max_{R_{min}}}`). |

```
$ git tag
v1.0 … v1.0.16
```

Pull v1.0.16, restart Expo:
- The RC error disappears.
- Section-3 "Tolerable Maximum Losses" tooltip now shows "top 8" (or whatever your p is).
- Nav icons follow the theme — the privacy-document shield inverts correctly, the fx glyph turns white in dark mode.
- Notations like `R₍min₎`, `R₍max₎`, `E_{R_{min}}`, `L_{max_{R_{min}}}` render with proper subscripts on mobile, matching the web build.

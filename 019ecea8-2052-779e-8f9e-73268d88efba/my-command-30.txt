I have added a comment which contains "last modified on YYYY-MM-DD" to the last line of every linked GitHub pages. You no longer need to parse the body for a date.

The 1x1 fallback web/favicon.ico is not replaced on the first run of mobile/scripts/[generate-assets.sh](http://generate-assets.sh) after npm install and also not on the second run after npm install pngjs.

Now, all the 4 menus, when opened, appear at the top end that in portrait mode, slip under the iPhone notch.

The edges of the up and down buttons of the input fields, the edges of the rightmost column, and the last bit of the text of Note slip under the iPhone notch, too.

The toggle switch of the Orientation Lock is now halfly like the toggle switch of the Theme. The knob is the same but the slider is like a thin rail, unlike the trough which is as wide as the knob.

The title of the Matrix Tableau reappears to have an explicit subtitle instead of the desired description in the form of toggletip. The leftmost column of the table is not freezed although the whole chunk of table on its right can be slided downward as a block.

The swipe is still done by scrollbar like last time, which is unsatisfactory.

I have uploaded an icon onto [https://raw.githubusercontent.com/Vviinncceenntt/icons/main/privacy-document-icon.svg](https://raw.githubusercontent.com/Vviinncceenntt/icons/main/privacy-document-icon.svg) as the fallback icon for Destination 3 of the bottom navigation. This should resolve the too large file size issue.

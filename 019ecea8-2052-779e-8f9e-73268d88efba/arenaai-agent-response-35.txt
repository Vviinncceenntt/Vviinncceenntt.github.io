A long list — let me work through each carefully and ensure consistency.

Lots to fix. Let me work through everything systematically.

## Item 1+3+4+6+9 — English text colours & sizes match Chinese; layout (variables below name); section title weight; bold numbers in highlight cells

This requires changes across both web and mobile builds. Strategy:
1. **English text** in field labels, settings, slider legend, footer, etc. now uses full `theme.text` colour (light: `#111`, dark: `#F2F2F2`) — same as Chinese.
2. **English font sizes** raised to match Chinese (close enough that they read at similar weight).
3. **Variable symbols** like `(n)`, `(Rmin - Rmax)` move **below** the English text, not to the right.
4. **Section titles** no longer bold (web uses `<h2>` by default — already bold; explicitly set `font-weight: 400`).
5. **Highlight cell numbers** not bold by default.

Let me start with web CSS:

Now I'll write a comprehensive new CSS:

## Item 5 — Reset Parameters + Manage Subscription buttons blue

The web Reset button currently uses `background:var(--warn)` and Manage uses border style. Make them all blue:

Now mobile: Reset (warn → primary), Manage Subscription (outline → primary):

## Item 7 — Functions content: bumpy English letters in mobile

The `functions.html` page is fetched live and rendered in a WebView. The "bumpy" letters means the source HTML's MathJax `<sub>` rendering is fine on a real browser but RN's WebView (especially older `@react-native-webview` versions) sometimes fails to load the MathJax script + fonts properly, falling back to system-rendered text. The fonts may also be subset or the page might use vertical-align CSS that RN's WebKit interprets differently.

This is largely **upstream behaviour** I can't directly fix. But I can inject a CSS reset that normalises text rendering:

Hook it into all three menus:

## Item 8 — Variable symbols (n), (p), (d) etc. below the English text, not on the right (mobile + web)

The mobile side: in `DashboardScreen.js`, `FieldRow` renders `nameEn + sym` on one Text node. Need to put sym on its own line. The web side: the `.label .en` includes `<span class="sym">` inline. Fix both.

Add `symLabel` style + update `enLabel` size:

Now web: the `<span class="sym">` is currently inline inside the `<span class="en">`. Let me check the actual render in app.js:

## Item 10 — Mobile bottom-nav: numbers in highlight cells & icon recolour on mobile

For mobile, the matrix highlight cells already use `fontWeight: '700'`. Per spec, NOT bold. Let me also set the colour to beige in dark mode (which my web `--grad-player-num` already does for web, mobile uses theme).

Now MatrixTable mobile: numbers in highlighted cells should NOT be bold:

## Item 11 — Menu titles horizontally centred at FULL modal width (not minus close button)

Mobile:

The web modal header is already updated in the new `styles.css`.

## Item 12 — Web bottom-nav active state (icon + label go blue)

Already covered in the new `styles.css` — `.botnav button.active { color: var(--primary); }` plus the icon filter. Let me also make sure `app.js` sets the `.active` class:

Also: when the modal is closed by the X button, the active class should clear. Let me find the close handler:

## Item — Other places using `theme.textSoft` for primary English labels — fix mobile

Now the OrientTriangle legend text (mobile) — also should use full text colour and bigger English:

Also the vertex letter colour for inactive vertices is `textSoft` — keep that since it's a distinct UI element. Now Section.js descZh:

Footnote on mobile:

## Item — Title NOT bold (except "Swiss Round Calculator" which IS bold)

The big app title IS supposed to be bold (as it always has been). Section subtitles should not be bold. Web's `.subtitle` already overrides `<h2>`'s default bold to `font-weight: 400`. Mobile's `<BiText>` doesn't set fontWeight so it defaults to 400 — good.

But wait — the spec says "All titles, except the 'Swiss Round Calculator', of all sections and need not to be bold". Confirmed our change aligns. Let me also make the Functions/Settings/Legal/Wong **menu titles** at the top of the modal — they're using `Chiron Hei HK` and shouldn't be bold either. Already set `fontWeight: '400'` in Modal.js update above. Web already uses `font-weight: 400` for `.modal header .titles .zh/.en` in the new CSS.

## Web matrix header: also fix the bold on highlight cells

The new CSS already sets `font-weight: 400` for `td.hi-player`, `td.hi-range`, `td.hi-both`. Good.

## Web grid layout — symbol now on its own line

Since I removed the sym from inside `.label .en`, I need to verify the grid still aligns. The grid is `1fr auto` — symbol now in column 1 still works, but visually the label cell is taller. Acceptable.

## Item 7 cont — Tooltip text needs proper colour too

Already fixed in CSS: `.tip-bubble .zh, .tip-bubble .en { color: var(--tooltip-text); }`.

Now sanity check all JS:

All 28 parse cleanly. Now let me also review for the **review/consistency** items the user requested.

## Cross-build consistency review

Things to ensure parity on:

1. **Title bold** — web `.title .zh/.en` has `font-weight: 700` ✓ ; mobile uses Chiron Sung HK font (no explicit weight, font itself is regular) — let me make mobile bold too:

Add `weight` prop so the app title can be bold:

Now apply `weight="700"` to the app title in both DashboardScreen and OnboardingScreen:

Sanity check:

All good. Now let me also handle the **review previous commands** suggestion. Re-reading the original spec carefully — I want to make sure things I added then removed are still done correctly:

1. ✅ Default values (n=193, p=8, d=4, r=12 placeholder) — verified
2. ✅ Throttle ~100ms — `NumberField.js` does this
3. ✅ Footnote underlines for both 黃汶聰 — fixed in v1.0.12
4. ✅ Title underline for 瑞士 — works via `underlineZh` prop
5. ✅ Tooltips for items have descriptions with template substitution — fixed
6. ✅ Copy Output buttons — work on Safari via execCommand fallback
7. ✅ HK-style traditional Chinese on top, British English below — yes throughout
8. ✅ Bottom nav 4 destinations with text labels — verified
9. ✅ Onboarding "I Agree" + "I Accept" (greyed) → Haptics → RC check → Paywall/Reminder/Dashboard — implemented
10. ✅ Section 5 paragraphs left-justified — `styles.para { textAlign: 'left' }` ✓
11. ✅ Matrix table buff/beige background — ✓
12. ✅ Note section title left-aligned — fixed in v1.0.17

Commit:

Update README and architecture-v1.md with version:

Commit:

Tagged **v1.0.18**.

## Summary

| # | Issue | Fix |
|---|-------|-----|
| 1 | English text dark/light grey everywhere | Every English label switched from `--text-soft` / `theme.textSoft` to full `--text` / `theme.text`. Web matrix headers, settings rows, tabs, toggle options, slider legend, footnote, en-only banner; mobile `BiOpt`, `OrientTriangle` legend, `Section` description, dashboard footnote. |
| 2 | English smaller than Chinese | Sizes raised to within ~1 px of the Chinese: web `.label .en` 14 px (was 13), `.tabs button .en` 12 px, `.set-row .label-bi .en` 13 px (was 12), mobile equivalents similarly bumped. |
| 3 | Variables `(n)`, `(p)`, etc. to the right of English | Restructured both builds so the symbol sits **below** the English line. Web `<span class="sym">` is now a direct grandchild of `.label`, not nested in `.label .en`. Mobile `FieldRow` renders `nameZh`, `nameEn`, then `sym` (or `<SymRange>`) as three stacked siblings. |
| 4 | Section titles bold | `.subtitle` (web) now `font-weight: 400`; modal header titles 400; mobile `<BiText>` defaults to `'400'` with new `weight` prop. Only the app title "Swiss Round Calculator" still uses `'700'` (web `.title`, mobile `weight="700"` explicitly). |
| 5 | Highlighted-cell numbers bold | Web `td.hi-*` and mobile `MatrixTable.js` body cells: `font-weight: 400` / `'400'`. |
| 6 | Highlighted-cell number colour | New: white in light mode, **beige (#F5F5DC)** in dark mode. Implemented in both web `:root` / `[data-theme="dark"]` variables and mobile `themes.js` LIGHT / DARK palettes. |
| 7 | Reset Parameters + Manage Subscription buttons | Web `Reset` no longer uses warn-orange; mobile `Reset` switched from `theme.warn` to `theme.primary`; mobile `Manage Subscription` switched from outline to filled primary. All Settings action buttons now uniformly blue. |
| 8 | Menu titles not centred on full modal width | Web `.modal header .titles` now `width: 100%; text-align: center` with the close button overlaid via `position: absolute`. Mobile Modal.js title block is `width: '100%'; alignItems: 'center'` with the close button overlaid likewise. |
| 9 | Web nav active state not blue | New CSS rule `.botnav button.active` sets primary colour on label; new filter chain `brightness(0) saturate(100%) invert(28%) sepia(99%) … hue-rotate(199deg)` re-tints the silhouette icon to primary blue. Web `app.js` toggles `.active` on tap and clears it when the modal closes. |
| 10 | Bumpy Functions letters on mobile WebView | New `TEXT_RENDER_FIX` injection sets consistent `body line-height`, forces `vertical-align: baseline` on inline elements, disables `font-feature-settings` so MathJax fonts don't shift glyphs vertically. Injected into all three menus. |
| 11 | Consistency review | Audited every previous `textSoft` usage and ensured only genuinely secondary captions still use it. Both builds parsed cleanly (28 JSX + 3 JS files). |

```
$ git tag
v1.0 … v1.0.18
```

Pull v1.0.18 — restart Expo (`npm run start`) and refresh Safari for the web preview. English labels should now appear at full intensity matching the Chinese, every variable like `(n)` sits below the English line, highlighted numbers are no longer bold and turn beige in dark mode, all Settings buttons are blue, and the web nav tab turns blue when its menu is open.
Summary of previous conversation
This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.

Goal
Continue developing the Swiss Round Calculator — a fully mobile-responsive, bilingual (Traditional Chinese Hong Kong style on top of British English, line-by-line, never slash-separated) calculator hosted at /home/user/swiss-round-calculator/. Two deliverables in separate folders:

mobile/ — React Native + Expo SDK 54 app (for iOS App Store + Google Play via EAS Build) with 30 HKD annual auto-renew subscription via RevenueCat.
web/ — Static HTML/CSS/JS (no ads, no auth, no RevenueCat), to be hosted on public server.
Author: 黃汶聰 / Man Chong Wong. Copyright © 2026.

Latest tag: v1.0.18 (18 patch releases done).

Instructions
Bilingual: traditional Chinese (HK style) on top of British English, line-by-line, never slash-separated.
User is on Windows 11, no Mac. iPhone 11 Pro with Expo Go 54.0.2 (SDK 54 only). Node 24.14.1, npm 11.14.1.
Always use npm run start etc. (local expo binary) NOT npx expo (which downloads SDK 56 and breaks Expo Go).
User wants iframe / WebView load live URL for menu contents — earlier alpha tried "Get Element + restyle" → font bouncing; base64 inline → bundle too big for Expo Go.
User keeps full TTF fonts in the repo (24.5MB ChironSungHK etc.) — do NOT subset; do NOT use WOFF2 (so same TTF can embed in SVG masters via @font-face).
Use font-display: block (matches user's GitHub Pages style.css).
DO NOT improvise content — every linked page now has <!-- webpage by Vincent last modified on YYYY-MM-DD --> HTML comment for cache invalidation.
Use lipsum only when both cache AND network are missing (LIPSUM constant in mobile/src/constants/lipsum.js).
Cache must mirror canonical GitHub Pages exactly — no hand-written fallback HTML.
All Settings action buttons must be the same primary blue.
English text in matrix header column must be at full table-text colour, not grey.
Highlighted matrix numbers: NOT bold; white in light mode, beige #F5F5DC in dark mode.
Section titles NOT bold except "Swiss Round Calculator" (the app title is bold).
Variable symbols (n), (p), (d), (r), (Rmin - Rmax), (ERmin - ERmax), (LmaxRmin - LmaxRmax), (Nmin - Nmax), (LmaxNmin - Lmax - LmaxNmax) must sit on their own line BELOW the English text, NOT to the right.
Menu titles must be centred on FULL modal width, not modal width minus close button.
Web bottom-nav active state should tint icon + label to primary blue (matches RN).
Note section (Section 5) titles "備註：" / "Note:" must be left-aligned.
User keeps p-writing.svg for web (browsers handle 89 KB radial gradients fine); use privacy-document-icon.svg deterministically for mobile (SvgXml silently produces 0×0 for p-writing.svg).
RevenueCat: 3-tier price cache (live → cache → fallback BASE_PRICE/BASE_CURRENCY = 30 HKD). Skip configure call entirely in Expo Go (Constants.appOwnership === 'expo').
Use expo-file-system/legacy import path (silence SDK 54 deprecation).
babel.config.js uses react-native-worklets/plugin (NOT react-native-reanimated/plugin).
App.js wrapped in <GestureHandlerRootView> + <SafeAreaProvider>; import 'react-native-gesture-handler' must be FIRST.
DashboardScreen detects Expo Go via Constants.appOwnership === 'expo' and skips PagerView (falls back to ScrollView).
All JSX must parse cleanly — verified each commit with @babel/parser from /tmp/jsx-check/check.js.
Discoveries
Dependency versions (pinned to SDK 54 bundledNativeModules.json)
expo ~54.0.34, react-native 0.81.5, react 19.1.0
expo-clipboard ~8.0.8, expo-constants ~18.0.13, expo-file-system ~19.0.23, expo-font ~14.0.12, expo-haptics ~15.0.8, expo-linear-gradient ~15.0.8, expo-screen-orientation ~9.0.9, expo-splash-screen ~31.0.13, expo-status-bar ~3.0.9
react-native-svg 15.12.1, react-native-webview 13.15.0, react-native-safe-area-context ~5.6.0
react-native-pager-view 6.9.1, react-native-gesture-handler ~2.28.0, react-native-reanimated ~4.1.1, react-native-worklets 0.5.1, react-native-screens ~4.16.0
react-native-purchases ^9.6.0, react-native-web ~0.21.0, @expo/metro-runtime ~6.1.2
@react-native-async-storage/async-storage 2.2.0, @react-native-community/netinfo 11.4.1
.npmrc: fund=false, audit=false, legacy-peer-deps=true, prefer-offline=true
Critical bug histories solved
react-native-reanimated 4.x requires react-native-worklets peer dep (not auto-installed). Babel plugin moved to react-native-worklets/plugin.
iOS RN Modal default supportedOrientations only includes launch orientation → crashes when opened in landscape-lock. Fixed with supportedOrientations={['portrait', 'portrait-upside-down', 'landscape', 'landscape-left', 'landscape-right']}.
ImageMagick convert on Windows often lacks SVG delegate → 4 fallback invocation patterns + Node WASM fallback via @resvg/resvg-js (helper dir must NOT start with dot — npm rejects).
generate-assets.sh recursively inlines remote <image href="…"> and @font-face url(…) via Node-based inliner, then unwraps nested <image href="data:image/svg+xml…"> into inline <svg> elements so resvg-js can see fonts.
Python's built-in http.server defaults to IPv6-only on Python 3.14 + reports wrong MIME for TTF → web/serve.py forces IPv4 + registers font/ttf.
Safari blocks navigator.clipboard.writeText on insecure origins → fallback to document.execCommand('copy') with iOS-specific selection dance.
JSX comments {/* */} cannot be placed as siblings of root in return ( … ); must use plain // … above.
SubSym subscripts: transform ignored on RN <Text>. Solution: render whole notation as ONE outer <Text> with nested smaller <Text> children — inline text nesting aligns on same baseline naturally.
HTML-comment date parser: matches <!--[^>]*?last\s+modified(?:\s+on)?\s*[:\-]?\s*(\d{4})-(\d{2})-(\d{2})[^>]*?-->
Wong system page style.css uses invalid format('ttf') → PEAK_FONT_FIX injects corrected format('truetype') rule.
Functions menu "bumpy letters" → TEXT_RENDER_FIX injection normalizes line-height, vertical-align: baseline, disables font-feature-settings.
Web favicon: SVG primary (modern browsers) + ICO fallback (legacy). Generated by generate-assets.sh via ImageMagick or Node + pngjs fallback.
Bottom nav icons (5 files in mobile/assets/icons/ + web/assets/icons/)
formula-fx-icon.svg (1591 bytes)
gear-icon-72a7cf.svg (958 bytes, has explicit fill="#72a7cf")
p-writing.svg (89376 bytes, 12 radial gradients — web uses it, mobile silently fails)
pasted-image-20240416184020.svg (7991 bytes — Wong system icon)
privacy-document-icon.svg (2159 bytes — used as Legal icon on mobile)
tintIcon(xml, color) in BottomNav.js replaces explicit black fills with currentColor AND adds fill="currentColor" color="<themeColor>" to root <svg> so default-fill icons follow theme.
Web nav active tint filter: brightness(0) saturate(100%) invert(28%) sepia(99%) saturate(2476%) hue-rotate(199deg) brightness(94%) contrast(101%) for light mode primary #0050B3; dark mode uses different filter chain for #5BA3FF.
Theme palette (mobile/src/theme/themes.js)
LIGHT: text #111, textSoft #333, primary #0050B3, gradPlayerNumber #FFFFFF, gradRangeNumber #FFFFFF
DARK: text #F2F2F2, textSoft #DDDDDD, primary #5BA3FF, gradPlayerNumber #F5F5DC, gradRangeNumber #F5F5DC

Linked page URLs (constants/strings.js URLS)
https://vviinncceenntt.github.io/swiss-round-calculator/privacy-policy.html
https://vviinncceenntt.github.io/swiss-round-calculator/eula.html
https://vviinncceenntt.github.io/swiss-round-calculator/terms.html
https://vviinncceenntt.github.io/swiss-round-calculator/disclaimer.html
https://vviinncceenntt.github.io/swiss-round-calculator/functions.html
https://vviinncceenntt.github.io/wongssystem.html
iOS subscriptions: https://apps.apple.com/account/subscriptions
Android subscriptions: https://play.google.com/store/account/subscriptions
User-Pasted Content
Most recent user message (request that drove v1.0.18)
I meant that the English texts (the variables in brackets) of the leftmost column of the table should not be dark gray (inconsistant with other texts) in light mode in web build and they should not light gray (too blurry to read) in dark mode in web build. Other English texts and the variables of the items in other sections and even the English texts in Settings are also dark gray in light mode and light gray in dark mode in web build. This is undesirable.

The numbers of the highlighted columns (red and blue only) of the table are bold in both light mode and dark mode in web build which they are not supposed to be.

All English texts should be in the same colour as Chinese texts and should have font sizes of similar height.

Please set the numbers of the highlighted columns to beige (#F5F5DC) in dark mode while keeping them white in light mode for both web build and mobile build.

The whole shield of the icon for Legal menu in mobile build is now black in light mode and white in dark mode. I think that this is good.

The English words "max", "min", "otherwise", "BinomInv", "prob" in "P_{prob}" in the fetched content of Functions look bumpy in mobile build. This does not happen in web build.

"(n)", "(p)", "(d)" in Tournament Parameters, and "(r)" in Reverse Round Lookup in mobile build and "(n)", "(p)", "(d)" in Tournament Parameters, "(Rmin - Rmax)", "(ERmin - ERmax)", "(LmaxRmin - LmaxRmax)" in Specific Results, and "(r)", "(Nmin - Nmax)", "(LmaxNmin - Lmax - LmaxNmax)" in Reverse Round Lookup in web build should not be on the right of the English texts but below them.

All titles, except the "Swiss Round Calculator", of all sections and need not to be bold for both web build and mobile build.

Set the Reset Parameters button (web build and mobile build) and the Manage Subscription button (mobile build) in Settings also blue so that all buttons are aligned.

All menu titles should be horizontally aligned at the center of its container width , not the width of container minus the close button.

In the bottom navigation, it is nice to see that the icon and the text label of the selected menu become blue in mobile build. However, I do not see that they become blue in web build.

Review web build and mobile build to check for consistency between them.

Review all my previous commands and your logs to find any proper things you have done in earlier versions but removed in later versions and judge what should be done.

Source GitHub style.css
Confirmed via fetch:

CSS

/* https://vviinncceenntt.github.io/style.css */
@font-face {
  font-family: 'The Peak Font Plus', '隨峰體Plus', 'ThePeakFontPlus', 'the-peak-font-plus', 'thepeakfontplus-regular';
  src: url('thepeakfontplus-regular.ttf') format('ttf');
  font-display: block;
}
body { font-family: 'The Peak Font Plus', ...; }
Note: format('ttf') is invalid W3C, should be format('truetype') — hence PEAK_FONT_FIX.

Sample page-end comment format
<!-- webpage by Vincent last modified on 2026-06-22 --> (from privacy-policy.html)

Accomplished
Completed
v1.0.0 → v1.0.18 all tagged; all 28 mobile JSX files + 3 web JS files parse cleanly via @babel/parser.
Web build verified working on iPhone Safari via python serve.py (port 8765, IPv4-bound).
Mobile build verified bundling successfully in Expo Go after v1.0.15 + v1.0.14 fixes.
v1.0.18 just shipped: full text colour everywhere for English, symbols below English not right, non-bold subtitles & highlight numbers, beige numbers in dark mode, primary-blue Reset+Manage buttons, full-width centred menu titles, web nav active tint via CSS filter chain, TEXT_RENDER_FIX injection for WebView bumpy letters.
Not Solved
User has NOT yet tested v1.0.18 — awaiting feedback on whether all 11 reported issues are actually fixed.
The "bumpy letters" fix is speculative — TEXT_RENDER_FIX may not fully solve it (it normalizes line-height + vertical-align, but root cause might be MathJax-specific). User may report it persists.
Color filter chain for web nav active state was computed manually — exact tint may not perfectly match #0050B3 / #5BA3FF. User may notice deviation.
PagerView swipe only works in EAS dev/prod builds, not Expo Go. User has been testing in Expo Go only.
Next Steps
Wait for user feedback on v1.0.18.
If "bumpy letters" persist, consider injecting a more aggressive font-rendering reset, or check if iOS WebView needs -webkit-font-smoothing: antialiased; -webkit-text-stroke: 0 etc.
If web nav active tint colour is off, may need a different precomputed filter chain (use https://codepen.io/sosuke/pen/Pjoqqp algorithm).
User mentioned EAS dev build path documented but hasn't tried it yet — may need walkthrough.
Relevant files / directories
Repo root
/home/user/swiss-round-calculator/

README.md, CHANGELOG.md, TODO.md, .gitignore, .env, .env.example
docs/architecture-v1.md (latest: v1.0.18)
Mobile (Expo SDK 54)
mobile/App.js — GestureHandlerRootView + SafeAreaProvider + initial landscape lock
mobile/package.json, mobile/app.json, mobile/eas.json, mobile/babel.config.js, mobile/.npmrc
mobile/assets/fonts/ — 4 TTFs (ChironSungHK, ChironHeiHK, ChironGoRoundTC, ThePeakFontPlus) — kept in repo per user
mobile/assets/icons/ — 5 SVGs listed above
mobile/assets/images/ — 7 PNGs (app-icon + 6 splashes) regen by generate-assets.sh
mobile/scripts/generate-assets.sh — SVG→PDF→PNG pipeline, copies app-icon.svg to web/, generates favicon.ico via ImageMagick or Node+pngjs
mobile/scripts/inline-icons.js — assets/icons/*.svg → src/constants/icons.js
mobile/scripts/README.md
mobile/src/constants/strings.js — bilingual dictionary + URLS + DEFAULTS (n=193, p=8, d=4, rPlaceholder=12)
mobile/src/constants/icons.js — auto-generated, 4 nav icon exports (privacyDocumentIcon used for Legal on RN)
mobile/src/constants/lipsum.js — LIPSUM map for offline+no-cache fallback
mobile/src/theme/themes.js — LIGHT/DARK (gradPlayerNumber/gradRangeNumber #FFFFFF light / #F5F5DC dark)
mobile/src/context/AppContext.js — theme, orient (auto/landscape/portrait), params, onboarded
mobile/src/utils/calculator.js — pure Swiss tournament maths
mobile/src/utils/revenuecat.js — 3-tier price cache, IS_EXPO_GO check
mobile/src/utils/pageCache.js — uses expo-file-system/legacy, HTML-comment date parser
mobile/src/utils/webviewDarkMode.js — INJECTED_DARK, INJECTED_LIGHT, PEAK_FONT_FIX, TEXT_RENDER_FIX
mobile/src/components/BiText.js — has weight prop (default '400')
mobile/src/components/BottomNav.js — uses tintIcon(), privacyDocumentIcon for Legal
mobile/src/components/Modal.js — useSafeAreaInsets, supportedOrientations, title centred full-width, close button overlay
mobile/src/components/MatrixTable.js — frozen first column outside scroller, single column-spanning gradient, NOT bold numbers
mobile/src/components/NumberField.js — strips leading zeros, clamps max on keystroke, enforces min on blur
mobile/src/components/OrientTriangle.js — single sliding knob, knob-wide trough (TROUGH = KNOB_R*2), legend at full text colour
mobile/src/components/Section.js — has titleAlign prop (default 'center'), titleTipZh/En for tooltip-style subtitle
mobile/src/components/SubSym.js — nested <Text> baseline alignment, SUB_RATIO 0.65
mobile/src/components/TipAnchor.js, TipOverlay.js, Tip.js
mobile/src/screens/OnboardingScreen.js — warmCache (all 6 pages with hasDate:true), warmRevenueCat, no config import
mobile/src/screens/Paywall.js — uses getAnnualPriceLabel()
mobile/src/screens/DashboardScreen.js — PagerView (real swipe in EAS build) or ScrollView fallback (Expo Go via Constants.appOwnership check), FieldRow renders zh/en/sym as three stacked siblings, weight="700" on title, titleAlign="left" on Section 5
mobile/src/screens/menus/FunctionsMenu.js — TEXT_RENDER_FIX added
mobile/src/screens/menus/LegalMenu.js — TEXT_RENDER_FIX added, 4 tabs PP/EULA/T&C/Disclaimer
mobile/src/screens/menus/SettingsMenu.js — Reset+Manage buttons primary blue, BiOpt at full text colour
mobile/src/screens/menus/WongMenu.js — PEAK_FONT_FIX + TEXT_RENDER_FIX
Web (static)
web/index.html — SVG favicon + ICO fallback, <h2 class="subtitle note-title"> for Note section, 4 nav buttons with <img class="ico">
web/serve.py — IPv4-bound HTTP server with correct MIME for TTF
web/favicon.ico, web/FAVICON.md
web/css/fonts.css — TTF-only with font-display: block
web/css/styles.css — all English text full --text colour, .subtitle font-weight: 400, sym below en (separate child of .label), highlight numbers not bold + beige in dark mode, modal titles centred full-width, .botnav .active gets primary tint via filter chain
web/js/calculator.js — mirror of mobile calculator
web/js/icons.js — base64 data URIs of 4 nav SVGs (includes pWriting + privacyDocumentIcon)
web/js/app.js — sym renders as own <span> outside <span class="en">, .active class toggled on bottom nav with close-clears, copyToClipboard with execCommand fallback
web/assets/fonts/ — 4 TTFs + README.md
web/assets/icons/ — 4 nav SVGs + app-icon.svg (favicon source)
External references (frequently checked)
Expo SDK 54 bundled modules: https://raw.githubusercontent.com/expo/expo/sdk-54/packages/expo/bundledNativeModules.json
Sandbox parser tool: /tmp/jsx-check/check.js (requires @babel/parser in /tmp/jsx-check/node_modules)
Git config: user.email "vviinncceenntt@users.noreply.github.com", user.name "Man Chong Wong"
Verified favicon generation: produces 32 KB MS Windows icon resource - 4 icons, 64x64, 32 bits/pixel, 48x48 via ImageMagick
Tag history (commits all done, never pushed)
v1.0, v1.0.1, v1.0.2, v1.0.3, v1.0.4, v1.0.5, v1.0.6, v1.0.7, v1.0.8, v1.0.9, v1.0.10, v1.0.11, v1.0.12, v1.0.13, v1.0.14, v1.0.15, v1.0.16, v1.0.17, v1.0.18


